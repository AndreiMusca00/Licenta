<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BookingApplication.ListPage">
    <ContentPage.Content>
        <StackLayout Margin="20">
            <Editor Placeholder="Enter Username" Text="{Binding Username}" HeightRequest="100" />
            <Label>Parola:</Label>
            <Entry x:Name="CampParola" Text="{Binding Password}" IsReadOnly="False" IsPassword="True"/>
            <Label>Mail:</Label>
            <Entry x:Name="CampMail" Text="{Binding Email}"/>
            <Label>Numar de telefon:</Label>
            <Entry x:Name="CampPhoneNumber" Text="{Binding PhoneNumber}" IsReadOnly="False"/>
            <Label>Nume:</Label>
            <Entry x:Name="CampNume" Text="{Binding Nume}"/>
            <Label>Prenume:</Label>
            <Entry x:Name="CampPrenume" Text="{Binding Prenume}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Text="Save" Clicked="OnSaveButtonClicked" />
                <Button Grid.Column="1" Text="Delete" Clicked="OnDeleteButtonClicked"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>