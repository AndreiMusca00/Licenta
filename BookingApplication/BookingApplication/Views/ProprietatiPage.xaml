<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:vm="clr-namespace:BookingApplication.ViewModels"
             x:Class="BookingApplication.Views.ProprietatiPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="ProfilulMeu" />

    </ContentPage.ToolbarItems>

    <ListView x:Name="listView" Margin="20" HasUnevenRows="True" ItemSelected="listView_ItemSelected">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Padding="5" Orientation="Horizontal" >
                         <Label Text="{Binding Nume}"></Label>
                        <StackLayout Padding="5">
                        <Label Text="{Binding Judet}"/>
                        <Label Text="{Binding Oras}"/>
                        <Label Text="{Binding Strada}"/>
                        </StackLayout>
                        <Label Text="{Binding Pret}"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>

        </ListView.ItemTemplate>
        </ListView>
</ContentPage>