<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BookingApplication.Views.ProfilulMeuIstoricRezervari">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="listView" Margin="20" HasUnevenRows="True" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Padding="5" Orientation="Horizontal" >
                            <Label Text="{Binding numeProprietate}"></Label>
                            <StackLayout Padding="5">
                                <Label Text="{Binding dataRezervare}"/>
                                <Label Text="{Binding orasProprietate}"/>
                                <Label Text="{Binding stradaProprietate}"/>
                            </StackLayout>
                            <Label Text="{Binding idRezervare}"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>